from setuptools import setup

# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="pygitstats",
    install_requires=[
        'gql>=3.0.0rc0'
    ],
    extras_require={
    },
)
